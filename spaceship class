
import java.awt.Rectangle;
import java.awt.Shape;
import java.awt.Polygon;
public class Ship{
	//make the ship
	public int[] shipx = {200, 215, 230}; //the x coordinates of the ship polygon
	public int[] shipy = {500, 460, 500}; //the y coordinates of the ship polygon
	// for the x values: {start spot, startspot + 15, startspot +30}
	//for the y values: {start spot, startspot - 40, start spot}
	
	private double velx, vely, locx, locy;
	boolean alive;
	private Shape shape;
	
	public Ship(){
		velx = 0;
		vely = 0;
		locx = 0;
		locy = 0;
		alive = true;
		Shape p = new Polygon(shipx, shipy, shipx.length);
		shape = p;
	}
	
	public void setVelX(double vel){velx = vel;}
	public void setVelY(double vel){vely = vel;}
	public void setLocX(double x){locx = x;}
	public void setLocY(double y){locy = y;}
	
	public double getVelX(){ return velx;}
	public double getVelY(){ return vely;}
	public double getLocX(){ return locx;}
	public double getLocY(){ return locy;}
	public int[] getPolygonX(){ return shipx; }
	public int[] getPolygonY(){ return shipy; }
	
	public Shape getShape(){
		return shape;
	}
	
	public Rectangle getBounds(){
		Rectangle r;
		r = new Rectangle((int) locx, (int) locy, 12, 12);
		return r;
	}
	
}
